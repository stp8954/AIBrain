services:
  vespa:
    image: vespaengine/vespa:latest
    container_name: vespa
    hostname: vespa-container
    ports:
      - "8080:8080"  # HTTP API
      - "19071:19071"  # Config server
    volumes:
      - vespa-data:/opt/vespa/var
    environment:
      - VESPA_CONFIGSERVERS=vespa-container
    healthcheck:
      # Check config server (19071) not data plane (8080) - data plane needs app deployed first
      test: ["CMD", "curl", "-f", "http://localhost:19071/state/v1/health"]
      interval: 30s
      timeout: 10s
      retries: 10
      start_period: 180s
    restart: unless-stopped

  nyrag:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nyrag
    ports:
      - "8000:8000"  # NyRAG API
    volumes:
      - nyrag-data:/app/data  # SQLite + uploads persistence
      - ./configs:/app/configs:ro  # Config files (read-only)
    environment:
      - VESPA_URL=http://vespa:8080
      - VESPA_CONFIG_URL=http://vespa:19071
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY:-}
      - OPENROUTER_MODEL=${OPENROUTER_MODEL:-google/gemini-3-flash-preview}
      - NYRAG_AUTO_DEPLOY=true  # Auto-deploy Vespa schema on startup
    depends_on:
      vespa:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/status"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    restart: unless-stopped

volumes:
  vespa-data:
    driver: local
  nyrag-data:
    driver: local
